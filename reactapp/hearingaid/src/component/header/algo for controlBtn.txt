if isControl == 0
	then show control component 
	then hide toppage component
	The name "control" changes to "back"
	isControl == !isControl
else
	show toppage component
	then hide control component 
	The name "back" changes to "control"
	isControl == !isControl



for this you need to learn prop drilling or context API.
	